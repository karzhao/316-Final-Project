import { describe } from 'vitest';

// Legacy placeholder tests are skipped to avoid connecting to a real DB during CI.
describe.skip('legacy mongo placeholder', () => {
    // Intentionally skipped.
});
